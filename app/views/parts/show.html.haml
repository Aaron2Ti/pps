- content_for :head do
  = stylesheet_link_tag 'paper'

= vrml(@part.wrl)
%br
= @part.desc
= link_to '编辑', edit_part_path(@part)
= link_to '删除', part_path(@part), :method => :delete
= link_to '返回', parts_path
