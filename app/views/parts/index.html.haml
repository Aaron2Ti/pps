所有模块
%table
  - @parts.each do |part|
    %tr
      %td= part.desc 
      %td= link_to '查看', part_path(part)
      %td= link_to '编辑', edit_part_path(part)
      %td= link_to '删除', part_path(part), :method => :delete
