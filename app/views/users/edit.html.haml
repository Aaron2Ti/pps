==编辑<b>#{@user.login}</b>账户的信息:
- form_for @user do |f|
  .field
    = f.label :name, '姓名:'
    = f.text_field :name
  .field
    = f.label :email, 'E-mail:'
    = f.text_field :email
  .field
    = f.label :password, '密码:'
    = f.password_field  :password
  .field
    = f.label :password_confirmation, '密码确认:'
    = f.password_field  :password_confirmation
  .submit
    = submit_tag '保存'
    = link_to '返回', :back
